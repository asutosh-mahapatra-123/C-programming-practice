#include <stdio.h>
int main()
{
    for (int a = 0; a <= 10; a++)
    {
        printf("the value of a is %d\n", a);
    }

    for (int b = 9; b; b--)
        printf("the value of b is %d\n", b);
    return 0;
}